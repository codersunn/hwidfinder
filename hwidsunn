#!/bin/bash

# Display the header
echo "============================="
echo "    HWID Finder by Codersunn  "
echo "============================="

# Get the HWID (for macOS)
HWID=$(ioreg -l | grep -i "IOPlatformUUID" | awk '{print $3}' | tr -d '"')

# Display the HWID in a highlighted square
echo "┌─────────────────────────────┐"
echo "│         Your HWID:         │"
echo "│         $HWID              │"
echo "└─────────────────────────────┘"
